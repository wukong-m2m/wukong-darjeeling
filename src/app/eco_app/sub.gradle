djappsource {
    eco_app {
        javaDependencies = [ 'base' ]
    }
}

project.tasks.createAppArchive.addPart("${project.rootDir}/app/eco_app/zeros.dat", djarchive_type_ecocast_capsule_buffer, null)
