djConfig {
	platform = 'arduino.avrora'
	libraries = ['bm_motetrack_c']
	app = 'emptyapp'
	appArchiveSize = 0 // We won't reprogramme the app anyway, so just use exactly the required amount without reserving space to upload future apps.
}

project.tasks.createAppArchive.addPart("${project.project.rootDir}/app/emptyapp/empty", 10, null)
project.tasks.createLibArchive.addPart("${project.project.rootDir}/app/emptyapp/empty", 10, null)

project.ext.cCompilerOptimisationLevel = '-O3'
